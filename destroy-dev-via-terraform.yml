# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:

- task: TerraformInstaller@1
  inputs:
    terraformVersion: 'latest'

- task: TerraformTaskV4@4
  inputs:
    provider: 'azurerm'
    command: 'init'
    backendServiceArm: 'Accelerate-NO-C23-BESTOS-UNITED (264a89f0-56e0-40ba-8d89-034174fddaab)'
    backendAzureRmResourceGroupName: 'BESTOS-RG'
    backendAzureRmStorageAccountName: 'sabestos'
    backendAzureRmContainerName: 'terraform'
    backendAzureRmKey: 'terraform.tfstate'

- task: CmdLine@2
  inputs:
    script: 'terraform workspace select dev'

- task: TerraformTaskV4@4
  inputs:
    provider: 'azurerm'
    command: 'destroy'
    environmentServiceNameAzureRM: 'Accelerate-NO-C23-BESTOS-UNITED (264a89f0-56e0-40ba-8d89-034174fddaab)'

